# 简单的技术指标计算
# 用于单只股票的指标分析

-- INPUT date:string, open:number, high:number, low:number, close:number, volume:number --
-- OUTPUT date:string, close:number, ma5:number, ma20:number, rsi:number --

# 移动平均线
close_window = window("close", 20)
ma5 = SMA(close_window, 5)
ma20 = SMA(close_window, 20)

# RSI指标
rsi = RSI(close_window, 14)

return [date, close, ma5, ma20, rsi]
