# 价格突破策略
# 使用内置仓位操作函数

-- INPUT date:string, close:number, volume:number --
-- OUTPUT signal:string --

# 获取前一天的收盘价
prev_close = close[-1]

# 价格上涨超过2%时买入
price_up = (prev_close != null and (close - prev_close) / prev_close > 0.02)

# 价格下跌超过2%时卖出
price_down = (prev_close != null and (close - prev_close) / prev_close < -0.02)

# 使用内置函数生成标准交易信号
signal = price_up ? BUY() : (price_down ? SELL() : "持有")

return signal
