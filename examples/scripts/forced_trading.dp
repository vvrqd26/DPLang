# 强制产生交易的测试策略
# 每5天轮换买卖

-- INPUT date:string, stock_code:string, open:number, high:number, low:number, close:number, volume:number --
-- OUTPUT signal:string --

# 获取当前行号（使用收盘价窗口的长度）
prices = window("close", 100)

# 简单规则：每5天切换一次
# 使用价格数值的个位数判断
last_digit = close - (close / 10) * 10

signal = (last_digit > 5) ? BUY() : SELL()

return signal
