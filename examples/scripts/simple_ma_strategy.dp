# 简单均线策略
# 当价格高于20日均线时买入，低于时卖出

-- INPUT date:string, close:number, volume:number --
-- OUTPUT signal:string --

# 计算20日均线
ma20 = MA(close, 20)

# 生成信号
signal = (ma20 != null and close > ma20) ? "买入" : (ma20 != null and close < ma20) ? "卖出" : "持有"

return signal
